prefix=/usr
includedir=${prefix}/include

sysconfdir=/etc
distconfdir=/usr/lib
module_directory=/lib/modules
module_signatures=PKCS7 legacy
module_compressions=zstd xz zlib

Name: kmod
Description: Tools to deal with kernel modules
Version: 34
Cflags: -I${includedir}
